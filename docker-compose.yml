services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-code_sentinel}
      POSTGRES_USER: ${POSTGRES_USER:-code_sentinel}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-code_sentinel}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

  back:
    build:
      context: ./apps/backend
    ports:
      - "8000:3000"
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USERNAME: ${POSTGRES_USER:-code_sentinel}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-code_sentinel}
      DB_NAME: ${POSTGRES_DB:-code_sentinel}
    depends_on:
      db:
        condition: service_healthy

  front:
    build:
      context: ./apps/frontend
    ports:
      - "3000:80"
    depends_on:
      - back

volumes:
  postgres_data: